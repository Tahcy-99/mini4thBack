spring:
  application:
    name: mini4thBack


  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: "jdbc:mariadb://10.10.10.30:3306/web"
    username: root
    password: qwer1234

  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org:
      springframework:
        security:
          web:
            FilterChainProxy: debug
        web:
          servlet:
            DispatcherServlet: debug
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace
